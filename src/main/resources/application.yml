spring:
  application:
    name: tontinepro

  datasource:
    url: jdbc:postgresql://dpg-daqa8oou01pc73fefgt0-a.frankfurt-postgres.render.com:5432/tontine_db_5rpr?sslmode=require&connectTimeout=30&socketTimeout=30
    username: tontine_db_5rpr_user
    password: IZ1cCKR2mhFWVdEmblZMd3BDbzjc8wQ7
    driver-class-name: org.postgresql.Driver
    hikari:
      initialization-fail-timeout: 60000
      connection-timeout: 30000

  jpa:
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        boot:
          allow_jdbc_metadata_access: false

  security:
    oauth2:
      client:
        registration:
          google:
            client-id: ${GOOGLE_CLIENT_ID:votre_client_id_temp_local}
            client-secret: ${GOOGLE_CLIENT_SECRET:votre_client_secret_temp_local}
            scope:
              - email
              - profile

server:
  port: ${PORT:8080}